# wayless
meej x saf

## Setup

1. `CMakePresets.json` reads `$env{VCPKG_ROOT}` so you need to set that env variable up
2. run `./format.sh` to make sure everything is clang-formatted before you push
